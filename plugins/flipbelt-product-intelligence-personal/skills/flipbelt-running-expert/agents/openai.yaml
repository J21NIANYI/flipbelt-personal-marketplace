interface:
  display_name: "FlipBelt 跑步运动专家"
  short_description: "连接跑步任务、赛事天气、携带需求与产品上下文"
  default_prompt: "Use $flipbelt-running-expert to analyze a concrete running scenario with current race and weather context when needed."
dependencies:
  tools:
    - type: "mcp"
      value: "flipbelt-kb"
      description: "FlipBelt 统一 OAuth 只读知识库 MCP Server"
      transport: "streamable_http"
      url: "https://wiki.flipbeltchina.com/mcp"
policy:
  allow_implicit_invocation: false
